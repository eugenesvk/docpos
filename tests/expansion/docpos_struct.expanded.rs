use docpos::*;
/// structPre line 1.
/// structPre line 2.
/// scructInner
pub struct StructyPos {
    /// doc @ x for x
    pub x: i8,
    /// doc @ x for y (after `///!`)
    pub y: i8,
}
/// structPre line 1.
/// structPre line 2.
/// scructInner
pub struct StructyPos2 {
    /// doc @ x for x
    pub x: i8,
    /// doc @ x for y (after `///!`)
    pub y: i8,
}